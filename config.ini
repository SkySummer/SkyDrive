# SkyDrive 配置文件

# 监听端口设置
port = 8080

# 日志等级配置 (DEBUG/INFO/WARNING/ERROR)
log_level = DEBUG

# 线程池大小设置
thread_count = 4

# 优雅关闭设置
linger = false

# 静态文件目录
static_dir = static

# 云盘文件存储目录（data 目录下）
drive_dir = files

# 用户账户信息文件名（data 目录下）
user_file = users.dat
